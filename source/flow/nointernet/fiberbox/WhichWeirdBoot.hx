package flow.nointernet.fiberbox;


//import flixel.addons.ui.StrNameLabel;
//import flow.nointernet.so.tckets._SwapBox;
import tstool.process.ActionDropDown;

/**
 * @todo ARCHIVE
 * @author bb
 */
class WhichWeirdBoot extends ActionDropDown 
{
	/*public function new() 
	{
		super([
			new StrNameLabel("", "Select ..."),
			new StrNameLabel("booting_issue_2", "Power green, Fiber red, no other LED"),
			new StrNameLabel("booting_issue_1", "All LAN LED green, Power green, Fiber red, no other LED "),
			new StrNameLabel("booting_issue_3", "All LED  1 sec, Power green, fiber red"),
			new StrNameLabel("non_stop_reboot", "Non stop reboot")
		]);
		
	}
	override public function create()
	{
		this._nextProcesses = [new _SwapBox()];
		super.create();
	}
	*/
}